# Cloudflare Pages — static site + /api/collection relay (functions/api/collection.js).
# Production: https://nakshatra-portal.pages.dev/infinity_nakshatra_dashboard
# Optional env var in Cloudflare dashboard: APPS_SCRIPT_EXEC_URL = your /exec URL

name = "nakshatra-portal"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."